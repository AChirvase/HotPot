<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/windowBackground</item>
        <item name="android:statusBarColor">@color/statusBarColor</item>
        <item name="android:windowLightStatusBar">true</item>

        <!-- Layout preview complains without this :S -->
        <item name="coordinatorLayoutStyle">@style/Widget.Support.CoordinatorLayout</item>
    </style>

    <style name="RoundEditText" parent="Widget.AppCompat.EditText">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@drawable/rectangle_round_corners</item>
        <item name="android:drawablePadding">20dp</item>
        <item name="android:paddingStart">16dp</item>
        <item name="android:padding">8dp</item>

    </style>

    <style name="IndicatorRatingBarSmall" parent="Widget.AppCompat.RatingBar.Small">
        <item name="android:progressBackgroundTint">@android:color/darker_gray</item>
        <item name="android:progressTint">@color/colorPrimary</item>
        <item name="android:secondaryProgressTint">@android:color/white</item>
        <item name="android:isIndicator">true</item>
    </style>

    <style name="IndicatorRatingBar" parent="Widget.AppCompat.RatingBar">
        <item name="android:progressBackgroundTint">@android:color/darker_gray</item>
        <item name="android:progressTint">@color/colorPrimary</item>
        <item name="android:secondaryProgressTint">@android:color/white</item>
        <item name="android:isIndicator">true</item>
    </style>


</resources>